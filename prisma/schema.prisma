datasource db {
  provider = "sqlite"
  url      = "file:./words.db"
}

generator client {
  provider = "prisma-client-js"
}

model WordEntry {
  id      Int    @id @default(autoincrement())
  word    String
  meaning String
}
