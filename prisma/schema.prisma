datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model WordEntry {
  id      Int    @id @default(autoincrement())
  word    String
  meaning String
}
